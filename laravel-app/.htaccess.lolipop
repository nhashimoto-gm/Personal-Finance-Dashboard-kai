# ロリポップ用 .htaccess（ルートディレクトリに配置）
# Laravel の public ディレクトリにリダイレクトします
#
# 使用方法:
# 1. このファイルを ~/web/.htaccess にコピー
# 2. 必要に応じてパスを調整

<IfModule mod_rewrite.c>
    RewriteEngine On

    # laravel-app/public にリダイレクト
    RewriteCond %{REQUEST_URI} !^/laravel-app/public/
    RewriteRule ^(.*)$ laravel-app/public/$1 [L]
</IfModule>
